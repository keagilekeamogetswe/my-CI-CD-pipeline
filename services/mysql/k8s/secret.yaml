apiVersion: v1
kind: Secret
metadata:
  name: mysql
  namespace: social-networks
type: Opaque
stringData:
  # Root credentials
  MYSQL_ROOT_PASSWORD: rootpassword123

  # App credentials
  MYSQL_DATABASE: social_test_db


  # Replication credentials
  MYSQL_REPLICATION_USER: repl_user
  MYSQL_REPLICATION_PASSWORD: replpassword123
